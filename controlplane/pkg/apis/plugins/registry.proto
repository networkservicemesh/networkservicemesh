syntax = "proto3";

package plugins;

import "ptypes/empty/empty.proto";

enum PluginFeature {
    CONNECTION = 0;
}

message PluginInfo {
    string endpoint = 1;
    repeated PluginFeature features = 2;
}

service PluginRegistry {
    rpc Register (PluginInfo) returns (google.protobuf.Empty);
}
