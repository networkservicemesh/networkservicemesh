version: 2
jobs:
  sanity-check:
    working_directory: /go/src/github.com/ligato/networkservicemesh
    docker:
      - image: circleci/golang:1.10
    steps:
      - checkout
      - run: sudo apt-get install yamllint shellcheck python3-pkg-resources
      - run: yamllint -c .yamllint.yml $(git ls-files '*.yaml' '*.yml' | grep -v 'vendor/')
      - run: make check dep-check

  build:
    working_directory: /go/src/github.com/ligato/networkservicemesh
    docker:
      - image: circleci/golang:1.10
    steps:
      - checkout
      - setup_remote_docker
      - run: go install ./vendor/k8s.io/kube-openapi/cmd/openapi-gen
      - run: make verify docker-build

  packet-deploy:
    working_directory: /cncf
    docker:
      - image: networkservicemesh/netmesh-ci
    steps:
      - run: ./circleci-provision.sh packet-deploy
      - run:
          command: kubectl create -f /cncf/rbac/
          environment:
            KUBECONFIG: /cncf/data/kubeconfig
      - persist_to_workspace:
          root: /cncf/data
          paths:
            - ./*

  packet-integration-tests:
    working_directory: /go/src/github.com/ligato/networkservicemesh
    docker:
      - image: networkservicemesh/netmesh-ci
    steps:
      - checkout
      - run: /cncf/set-nameserver.sh
      - attach_workspace:
          at: /cncf/data
      - run:
          command: ./scripts/circle-integration-tests.sh
          environment:
            KUBECONFIG: /cncf/data/kubeconfig
      - run:
          when: always
          command: |
            cd /cncf
            ./circleci-provision.sh packet-destroy

workflows:
  version: 2
  build-and-test:
    jobs:
      - sanity-check
      - build:
          requires:
            - sanity-check
      - packet-deploy:
          requires:
            - sanity-check
      - packet-integration-tests:
          requires:
            - build
            - packet-deploy
