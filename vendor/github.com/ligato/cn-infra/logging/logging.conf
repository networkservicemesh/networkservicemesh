# Set default config level for every plugin
default-level: debug

# Specifies a list of named loggers with respective log level
loggers:
  - name: "agentcore",
    level: info
  - name: "status-check",
    level: debug
  - name: "linux-plugin",
    level: info